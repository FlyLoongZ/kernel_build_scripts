From 47d6965bc7bad124028f6f42864f4f5bb095bd90 Mon Sep 17 00:00:00 2001
From: FlyLoongZ <60963681+FlyLoongZ@users.noreply.github.com>
Date: Sun, 19 Jan 2025 00:53:29 +0800
Subject: [PATCH] ztc1997 build fix

---
 arch/arm64/configs/gki_defconfig |  1 -
 scripts/setlocalversion          | 14 +++++++-------
 2 files changed, 7 insertions(+), 8 deletions(-)

diff --git a/arch/arm64/configs/gki_defconfig b/arch/arm64/configs/gki_defconfig
index ecbce5d6a..bcf4956f8 100644
--- a/arch/arm64/configs/gki_defconfig
+++ b/arch/arm64/configs/gki_defconfig
@@ -1,4 +1,3 @@
-CONFIG_LOCALVERSION="-ztc1997"
 CONFIG_AUDIT=y
 CONFIG_NO_HZ=y
 CONFIG_HIGH_RES_TIMERS=y
diff --git a/scripts/setlocalversion b/scripts/setlocalversion
index 842936656..963355a75 100755
--- a/scripts/setlocalversion
+++ b/scripts/setlocalversion
@@ -98,7 +98,7 @@ scm_version()
 			# again using exactly 12 hex chars.
 			else
 				head="$(echo $head | cut -c1-12)"
-				printf '%s%s' -g $head
+				printf '%s%05d%s%s' - $((RANDOM % 128 + 1)) -g $head
 			fi
 		fi
 
@@ -113,12 +113,12 @@ scm_version()
 		# it fails. Note that git-diff-index does not refresh the
 		# index, so it may give misleading results. See
 		# git-update-index(1), git-diff-index(1), and git-status(1).
-		if {
-			git --no-optional-locks status -uno --porcelain 2>/dev/null ||
-			git diff-index --name-only HEAD
-		} | grep -qvE '^(.. )?scripts/package'; then
-			printf '%s' -dirty
-		fi
+		##if {
+		##	git --no-optional-locks status -uno --porcelain 2>/dev/null ||
+		##	git diff-index --name-only HEAD
+		##} | grep -qvE '^(.. )?scripts/package'; then
+		##	printf '%s' -dirty
+		##fi
 
 		# All done with git
 		return
-- 
2.48.1

