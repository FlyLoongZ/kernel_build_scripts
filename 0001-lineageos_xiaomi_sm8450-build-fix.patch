From fc6c287b9274a7c07d399251fbba09aea66e54ad Mon Sep 17 00:00:00 2001
From: FlyLoongZ <60963681+FlyLoongZ@users.noreply.github.com>
Date: Fri, 21 Mar 2025 21:30:55 +0800
Subject: [PATCH] lineageos_xiaomi_sm8450 build fix

---
 android/gki_aarch64_modules      |  1 +
 arch/arm64/configs/gki_defconfig |  1 +
 scripts/setlocalversion          | 22 +++++++++++-----------
 3 files changed, 13 insertions(+), 11 deletions(-)

diff --git a/android/gki_aarch64_modules b/android/gki_aarch64_modules
index e69de29bb2d..2a1b69f651b 100644
--- a/android/gki_aarch64_modules
+++ b/android/gki_aarch64_modules
@@ -0,0 +1 @@
+drivers/soc/qcom/sync_fence/qcom_sync_file.ko
diff --git a/arch/arm64/configs/gki_defconfig b/arch/arm64/configs/gki_defconfig
index c79d77da33b..2f92fbcfe46 100644
--- a/arch/arm64/configs/gki_defconfig
+++ b/arch/arm64/configs/gki_defconfig
@@ -226,6 +226,7 @@ CONFIG_IP6_NF_FILTER=y
 CONFIG_IP6_NF_TARGET_REJECT=y
 CONFIG_IP6_NF_MANGLE=y
 CONFIG_IP6_NF_RAW=y
+CONFIG_IP6_NF_NAT=y
 CONFIG_TIPC=y
 CONFIG_L2TP=y
 CONFIG_BRIDGE=y
diff --git a/scripts/setlocalversion b/scripts/setlocalversion
index 842936656b8..b0edce2722a 100755
--- a/scripts/setlocalversion
+++ b/scripts/setlocalversion
@@ -91,15 +91,15 @@ scm_version()
 			# --abbrev=12 ensures a minimum of 12, and the
 			# awk substr() then picks the 'g' and first 12
 			# hex chars.
-			if atag="$(git describe --abbrev=12 2>/dev/null)"; then
-				echo "$atag" | awk -F- '{printf("-%05d-%s", $(NF-1),substr($(NF),0,13))}'
+			#####if atag="$(git describe --abbrev=12 2>/dev/null)"; then
+			##	echo "$atag" | awk -F- '{printf("-%05d-%s", $(NF-1),substr($(NF),0,13))}'
 
 			# If we don't have a tag at all we print -g{commitish},
 			# again using exactly 12 hex chars.
-			else
+			##else
 				head="$(echo $head | cut -c1-12)"
-				printf '%s%s' -g $head
-			fi
+				printf '%s%05d%s%s' - $(( ( $(od -An -N1 -tu1 < /dev/urandom) % 113 ) + 16 )) -g $head
+			#####fi
 		fi
 
 		# Is this git on svn?
@@ -113,12 +113,12 @@ scm_version()
 		# it fails. Note that git-diff-index does not refresh the
 		# index, so it may give misleading results. See
 		# git-update-index(1), git-diff-index(1), and git-status(1).
-		if {
-			git --no-optional-locks status -uno --porcelain 2>/dev/null ||
-			git diff-index --name-only HEAD
-		} | grep -qvE '^(.. )?scripts/package'; then
-			printf '%s' -dirty
-		fi
+		#####if {
+		##	git --no-optional-locks status -uno --porcelain 2>/dev/null ||
+		##	git diff-index --name-only HEAD
+		##} | grep -qvE '^(.. )?scripts/package'; then
+		##	printf '%s' -dirty
+		#####fi
 
 		# All done with git
 		return
-- 
2.49.0

